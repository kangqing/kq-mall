server:
  port: 8100

spring:
  application:
    name: kq-admin
  cloud:
    nacos:
      discovery:
        server-addr: http://yunqing.xyz:8848
        namespace: prod_namespace_id
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        file-extension: yaml
        namespace: prod_namespace_id
